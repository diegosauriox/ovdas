version: '3'

services:
  web:
    restart: always
    build: ./web
    expose:
      - "8000"
    env_file:
      - ./.envs/.mysql
    volumes:
      - .:/code
    command: /usr/local/bin/gunicorn docker_django.wsgi:application -w 2 -b :8000

